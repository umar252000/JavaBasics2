package com.umar.registerform.userbean.usersecurity;


import org.springframework.context.annotation.Bean;

import org.springframework.context.annotation.Configuration;
public class UserSecurity {

	/*
	 * @Bean public UserDetailsService userDetailsService() { UserDetails user =
	 * User.builder() .username("umar") .password("{noop}umar25") // {noop} is used
	 * for plain text password, remove it for encoded passwords .roles("ADMIN") //
	 * Note: roles are prefixed with "ROLE_" by Spring Security .build(); return new
	 * InMemoryUserDetailsManager(user); }
	 */
}
